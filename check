
#!/bin/bash
set -x

sudo python3 -m pip install yapf
pushd ~
rm -f .vim .Xresources
ln -s .config/nvim/_vim .vim
ln -s .config/nvim/_Xresources .Xresources
ln -s .config/nvim/_style.yapf .style.yapf
mkdir -p .ctags
popd
